import React from "react";

let Navbar = props => (
  <nav className="navbar is-light">
    <div className="navbar-brand">
      <div className="navbar-item">PhoneRecommender</div>
    </div>
  </nav>
);

export default Navbar;
